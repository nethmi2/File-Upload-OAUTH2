# File-Upload-OAUTH2

## Installation
Run the below command to install the package manager

`$ npm install`

## Deployment
Run the application on server by exceuting

`$ npm run prod`

This command will run node index.js
